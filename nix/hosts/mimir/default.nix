# Personal VM/Home Manager Configuration
# Minimal configuration for personal VMs or non-NixOS Linux systems

{ config, pkgs, lib, host, ... }:

{
  # Personal VM-specific packages
  home.packages = [
  ];
}
